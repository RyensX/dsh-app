{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "bundled",
  "description": "Local bootstrap page for the Bundled edition.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
